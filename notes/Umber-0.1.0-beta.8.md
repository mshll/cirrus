
- Umber is now signed with an Apple Developer ID and notarized by Apple. A fresh download opens straight away, with no warning to work around.
- Add Paste Mode. Start it from ⌘K, a global shortcut, or by typing `paste` on a line of its own, and everything you copy is added to the end of the open note until you stop it. It keeps working while the panel is hidden: the pill then appears at the top of the screen you are on. The footer shows the count and stops the mode when clicked. Passwords and copies marked transient are skipped.

