
- Umber draws its own tooltips instead of the native help tag, so a hint appears at once and on glass that matches the panel.
- Empty notes no longer pile up. An unnamed note left blank is deleted when you move to another note, and Move to Trash on any blank note deletes it outright instead of filling Recently Deleted.
- Toasts for deletions and other cautions now use the accent tint; only success and error are coloured.
- Choose in General settings whether Browse lists notes by when they were last opened or last edited. Pinned notes stay at the top.
- Go back and forward through the notes you opened this session with ⌘[ and ⌘], or from ⌘K.
- Move the panel to a corner, an edge, or the centre of the screen from "Move Panel…" in ⌘K.
- Fix a task item left empty reading as a bullet.
- Fix a group title scrolling out of view in ⌘K when its first row was selected.
- Fix a toast staying open after the pointer left it.

